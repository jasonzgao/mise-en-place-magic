{
  "name": "mise-en-place-magic",
  "version": "1.0.0",
  "description": "A Chrome extension that simplifies recipe webpages with intelligent mise-en-place instructions",
  "scripts": {
    "start-backend": "cd backend && npm start",
    "dev-backend": "cd backend && npm run dev",
    "generate-icons": "node scripts/generate-icons.js",
    "install-deps": "npm install && cd backend && npm install",
    "build": "mkdir -p dist && zip -r dist/mise-en-place-magic.zip manifest.json src LICENSE README.md -x \"**/node_modules/*\" -x \"**/.git/*\""
  },
  "keywords": [
    "chrome-extension",
    "recipe",
    "mise-en-place",
    "openai",
    "cooking"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "zip-dir": "^2.0.0"
  }
} 